// List some of the benefits of using TypeScript

// 1. TypeScript has code completion and intelligence.
// 2. It provides the benefits of optional static typing. TypeScript provides types that can be added to variables, function and classes.
// 3. TypeScript has the ability to compile down to a versionf of JavaScript that runs on all browsers. (modify the ts.config.json option)
// 4. TypeScript can catch errors during compile time instead of at runtime.
// 5. TypeScript compiles to JavaScript but contains tools to help find errors in object types for example.
// 6. TypeScript offers a compiler taht can convert to JavaScript code and has a concept of namespace defined by a "module".
